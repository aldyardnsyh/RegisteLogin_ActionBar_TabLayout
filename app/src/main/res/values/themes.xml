<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RegisteLogin_ActionBar_TabLayout" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Base application theme with ActionBar. -->
    <style name="Base.Theme.RegisteLogin_ActionBar_TabLayout.WithActionBar" parent="Theme.Material3.DayNight">
        <item name="android:statusBarColor">@color/purple</item>
        <item name="android:fontFamily">@font/poppins_regular</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Themes that can be applied to the application or specific activities. -->
    <style name="Theme.RegisteLogin_ActionBar_TabLayout" parent="Base.Theme.RegisteLogin_ActionBar_TabLayout.WithActionBar" />
</resources>